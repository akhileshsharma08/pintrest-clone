/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','images.ctfassets.net','in.pinterest.com','picsum.photos'],
      }
  
};

export default nextConfig;
